<div class="tag">
	<% if policy(stub).tag? %>
		<%= link_to "<span>X</span>".html_safe, remove_stub_tag_path(stub, tag), method: :delete, remote: true, class: "remove", title: "remove" %>
	<% end %>
	<%= tag.name %>
</div>